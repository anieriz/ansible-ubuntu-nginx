---
dist: xenial
language: python
python: 2.7
sudo: required
services:
  - docker
before_install:
  - sudo apt-get update
  - sudo apt-get install -y python-apt python-apt-common python-dev
install:
  - pip install molecule docker
script:
  - molecule test