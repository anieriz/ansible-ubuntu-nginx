---
dist: xenial
language: python
sudo: required
services:
  - docker
before_install:
  - sudo apt-get update
  - sudo apt-get install -y python3-apt python-apt-common
install:
  - pip install molecule docker
script:
  - molecule test