---
dist: xenial
language: python
python: 2.7
sudo: required

services:
  - docker

before_install:
  - sudo apt-get update
  - pip install --upgrade setuptools

install:
  - pip install molecule
  - pip install docker
  - pip install docker-py
  - pip install python-apt

script:
  - molecule test