---
dist: xenial
language: python
python: 2.7
sudo: required

services:
  - docker

before_install:
  - sudo apt-get update
  - sudo apt-get install -y python python-pip python-apt
  - pip install --upgrade setuptools wheel

install:
  - pip install molecule ansible docker

script:
  - molecule test