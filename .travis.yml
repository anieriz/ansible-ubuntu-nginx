---
dist: xenial
language: python
python: 2.7
sudo: required

services:
  - docker

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y python-apt

install:
  - pip install molecule
  - pip install docker

script:
  - molecule test